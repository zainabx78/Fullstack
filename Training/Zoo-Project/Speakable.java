package com.sparta.zf.oop.zoo;

// INTERFACE --> // Only have to put return type, name and arguments it takes.
    // 100% abstract class
    // No concrete implementation of any methods.

    
public interface Speakable {
    String speak();
}
